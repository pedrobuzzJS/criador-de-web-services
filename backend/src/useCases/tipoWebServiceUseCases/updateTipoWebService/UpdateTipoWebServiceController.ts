import { Request, Response } from "express";

class UpdateTipoWebServiceController {
    static async handle(request: Request, response: Response) {
        
    };
};

export default UpdateTipoWebServiceController;